function Logo(){
    return (
        <div className="logo">
            {/* Images importées depuis la balise img sont accessibles dans 'public', comme si on était dans ce dossier, quel que soit le fichier
            d'où on importe*/}
            <h3>World Flags</h3>
        </div>
    )
}

export default Logo